# Agent rules

- Three apps in `apps/` (`customer`, `admin`, `platform`), one shared package in `packages/shared/`. Do not introduce a fourth app or move code outside this structure without asking.
- Tailwind v4 — there is no `tailwind.config.ts`. The `@theme` block lives in [`packages/shared/src/tokens/tokens.css`](packages/shared/src/tokens/tokens.css). Tokens are added there, not in JS config.
- Backend is FastAPI in a separate repo. The contract is [`docs/openapi.yaml`](docs/openapi.yaml). When you change request/response shapes in `packages/shared/src/types/`, update the spec in the same change.
- Customer app must stay unauthenticated. Admin app authenticates by email/password (`POST /auth/login`) and sends a short-lived JWT as `Authorization: Bearer`, with a refresh token handled by the shared `client.ts` (single-flight refresh on 401). The platform app sends an `x-platform-token` header, also attached by `client.ts`. There are no session cookies; the customer app carries no credentials at all.
- FCM is admin-only. Do not import Firebase into `apps/customer`.
- Subscription plans differ by **capacity, not capability** — every tier gets the whole product. Two axes only: monthly order volume (Lite 10,000, everyone else uncapped) and locations (Lite and Standard 1, Pro uncapped). Staff accounts and tables are unlimited on every tier. Do not gate a feature behind a tier, and do not reintroduce a staff or table cap — both were proposed and dropped, because they bite during service. Caps live in [`apps/platform/src/tiers.data.ts`](apps/platform/src/tiers.data.ts); the rationale is in [`docs/plans.md`](docs/plans.md).
- Color usage is governed by [`docs/color-usage.md`](docs/color-usage.md) — the canonical surface/elevation rule for all three apps, now Material 3 (DS v3, solved from the seed in HCT). In short — page & top nav: `bg-surface` (now warm near-white `#FFF8F5`, **lighter** than cards); card/sheet/drawer/rail: `bg-surface-container-low`; nested block inside a card: `bg-surface-container`; modal/dialog/menu/tooltip: `bg-surface-container-high`; hover: one step up. Chips are outlined at rest and take `secondary-container` when selected. Filled buttons use the seed `bg-primary` #F56500 with a **16px semibold** white label — white is 3.11:1 on it, which is AA for large text only, so body-size brand text uses `text-primary-label` (P40, 6.16:1). Neutrals are warm: Oshap grey is gone. Never hardcode hex or use a raw palette step for a surface.
